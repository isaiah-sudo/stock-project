# Optional: point at a local backend while developing the frontend in isolation.
# When the frontend runs on Fly, leave this unset so requests stay same-origin at /api.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000/api
