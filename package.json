{
  "name": "financial-advisor-app",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ],
  "scripts": {
    "dev": "npm run dev --workspace backend & npm run dev --workspace frontend",
    "dev:frontend": "npm run dev --workspace frontend",
    "dev:backend": "npm run dev --workspace backend",
    "dev:quotes": "python -m uvicorn quote_service:app --app-dir python-quote-service --host 0.0.0.0 --port 8001",
    "start:all": "npx concurrently \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\" \"npm run dev:quotes\"",
    "build": "npm run build --workspace shared && npm run build --workspace backend && npm run build --workspace frontend"
  }
}
